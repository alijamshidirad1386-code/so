# SECURITY-NOTES

این پروژه یک سایت استاتیک است و داده‌ی فرم رزرو را فقط در `localStorage` مرورگر نگه می‌دارد. هیچ endpoint یا دیتابیس خارجی در کد تعریف نشده است.

برای جلوگیری از هشدارهای mixed-content، همه‌ی assetهای اصلی محلی هستند و CSP پایه نیز در `index.html` قرار گرفته است.

در GitHub Pages، HTTPS را فعال نگه دار. اگر بعد از انتشار روی یک دامنه‌ی سفارشی هشدار امنیتی دیدی، وضعیت همان URL را در Google Safe Browsing بررسی کن؛ هشدار دامنه می‌تواند مستقل از کد سایت باشد.
